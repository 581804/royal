'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import '../admin.css';
export default function Login(){const [p,setP]=useState('');const [e,setE]=useState('');const r=useRouter();return <div className="login"><div className="loginCard"><img src="/logo.png"/><p>ADMINISTRATION</p><h1>Royal Vastra</h1><input type="password" placeholder="Admin password" value={p} onChange={x=>setP(x.target.value)}/><button onClick={async()=>{const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(res.ok)r.push('/admin');else setE('Invalid password')}}>ENTER</button>{e&&<small>{e}</small>}</div></div>}
